--- reveal_agx_direct_user_clip_onehot_experiment.swift
+++ reveal_agx_direct_user_clip_zero_crossing_experiment.swift
@@ -80,19 +80,22 @@
     (void)geometryBits;
     const bool isOuter = !isRight;
     const float viewport = metadata[caseIndex].x == 0u ? 256.0f : 512.0f;
-    const float x = isRight ? viewport * 0.75f : viewport * 0.25f;
-    const float y = isBottom ? viewport * 0.25f : viewport * 0.75f;
+    (void)viewport;
     const float denominator = 65536.0f + float(metadata[caseIndex].z);
+    const float distanceValue = isOuter ? -1.0f : denominator - 1.0f;
+    const float x = distanceValue * 0x1p-17f;
+    const float y = isBottom ? 0.5f : -0.5f;
     const uint local = corner % 3u;
     const float3 oneHot = local == 0u ? float3(1.0f, 0.0f, 0.0f)
                         : local == 1u ? float3(0.0f, 1.0f, 0.0f)
                                       : float3(0.0f, 0.0f, 1.0f);
     BasisVertexOutput output;
-    output.position = mvp * float4(x, y, 0.0f, 1.0f);
+    (void)mvp;
+    output.position = float4(x, y, 0.0f, 1.0f);
     (void)outerBits;
     (void)innerBits;
     output.basis = float4(oneHot, dot(oneHot, float3(1.0f, 2.0f, 4.0f)));
     output.caseIndex = caseIndex;
-    output.clipDistance = isOuter ? -1.0f : denominator - 1.0f;
+    output.clipDistance = distanceValue;
     return output;
 }
