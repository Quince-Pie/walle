Run `bash tests/lifecycle/build.sh`, then `python tests/lifecycle/run.py --binary build/bin/release/walle`. Requires Labwc, grim, NumPy/Pillow, Wayland scanner and the app build dependencies. Run separately from GPU measurements. No live compositor or service is used. The runner checks the actual app child status, observed worker/timer events, fresh pixels, and Vulkan checked teardown. See the retained research result for original qualification. This is an opt-in GPU test, not part of CPU-only material checks.
